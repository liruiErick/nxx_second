(function () {

    var SPA_RESOLVE_INIT = function (hashData) {

        console.log('开始执行indexScienceRes.js');

        indexScienceRes(hashData);

    };

    function indexScienceRes(hashData) {

        // console.log(hashData);

        // do something



    }

    return window.SPA_RESOLVE_INIT = SPA_RESOLVE_INIT

})();