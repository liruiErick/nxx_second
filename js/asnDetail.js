/**
 * Created by lirui on 2017/10/9.
 */
(function () {
    var SPA_RESOLVE_INIT = function (hashData) {

        console.log('开始执行asnDetail.js');

        asnDetail(hashData);

    };

    function asnDetail(hashData) {

        // console.log(hashData);

        // do something



    }

    return window.SPA_RESOLVE_INIT = SPA_RESOLVE_INIT

})();